language: julia
os:
    - linux
    # - osx
julia:
    - 0.7
    - 1.0
notifications:
    email: false
sudo: false
# addons:
#     apt_packages:
#         - gfortran
#         - liblapack-dev
#         - libblas-dev
script:
    - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
    # - julia --color=yes -e 'if VERSION >= v"0.7.0-" using Pkg; end; Pkg.clone(pwd()); Pkg.test("SDCO"; coverage=true)';
    - julia --color=yes -e "using Pkg; Pkg.build(); Pkg.test()"
after_success:
    - echo $TRAVIS_JULIA_VERSION
    - julia --color=yes -e 'using Pkg; Pkg.add("Coverage"); cd(Pkg.dir("SDCO")); using Coverage; Coveralls.submit(process_folder()); Codecov.submit(process_folder())'
    # - julia -e 'Pkg.add("Documenter")'
    # - julia -e 'cd(Pkg.dir("JuMP")); include(joinpath("docs", "make.jl"))'